<%- include('../partials/head') %>
  <body>
    <%- include('../partials/nav') %>
    
    <main>
      <div>
        <section class="py-10 bg-gray-50 sm:py-16 lg:py-14">
          <div class="px-4 mx-auto sm:px-6 lg:px-8 max-w-7xl">
            <div class="max-w-2xl mx-auto text-center">
              <h2 class="text-3xl font-bold leading-tight text-black sm:text-4xl lg:text-5xl">Here are Your Registered Houses</h2>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mt-12">
              <% registeredHouse.forEach(house => { %>
                <div class="p-6 bg-white rounded-xl shadow-lg hover:shadow-2xl transition-all duration-300">
                  <a href="#" class="block overflow-hidden rounded-lg">
                    <img class="object-cover w-full h-64 transition-transform duration-300 hover:scale-105" src="<%= house.housePic %>" alt="<%= house.houseName %>" />
                  </a>
                  
                  <h3 class="mt-6 text-xl font-bold text-gray-900 hover:text-blue-600 transition-colors">
                    <a href="#"><%= house.houseName %></a>
                  </h3>
                  <p class="mt-4 text-gray-600"><span class="font-medium">Location:</span> <%= house.houseLocation %></p>
                  <p class="mt-2 text-2xl font-bold text-blue-600">₹<%= house.housePrice %> <span class="text-sm font-normal text-gray-600">/night</span></p>
                    <a href="/host/edit-house/<%= house._id %>?editing=true" class="mt-4 block text-center w-full bg-green-600 text-white py-2 px-4 rounded-lg hover:bg-green-700 transition-colors cursor-pointer duration-300 font-medium">Edit</a>
                  <form action="/host/delete-house/<%= house._id %>" method="post">    <button class="mt-4 w-full bg-red-600 text-white py-2 px-4 rounded-lg hover:bg-red-700 transition-colors cursor-pointer duration-300 font-medium">Delete</button></form>
              
                  <div class="h-0 mt-6 mb-4 border-t-2 border-gray-100"></div>
                  <div class="flex items-center">
                    <span class="text-sm font-medium text-gray-700">Rating:</span>
                    <span class="ml-2 text-yellow-500 text-lg"><%= house.houseRating %>⭐</span>
                  </div>
                </div>
              <% }) %>
            </div>
          </div>
        </section>
      </div>
    </main>
  </body>
</html>
